---

trivial:
  - >
    molecule test code - style only, no functional change: use FQCN for the
    remaining bare filter, test, and lookup plugin references (e.g.
    ``is ansible.builtin.mapping``, ``lookup('ansible.builtin.env', ...)``).
